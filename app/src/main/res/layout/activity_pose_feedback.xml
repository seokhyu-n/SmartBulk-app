<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 카메라 미리보기 (맨 뒤) -->
    <androidx.camera.view.PreviewView
        android:id="@+id/cameraPreview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000"
        android:elevation="0dp" />

    <!-- 스켈레톤 오버레이 (카메라 위) -->
    <com.example.smartbulk.OverlayView
        android:id="@+id/overlayView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:elevation="2dp" />

    <!-- 현재 측정 중인 운동 이름 -->
    <TextView
        android:id="@+id/exerciseNameText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_marginTop="32dp"
        android:layout_marginStart="16dp"
        android:gravity="center"
        android:text="운동"
        android:textColor="@color/text_primary"
        android:textSize="13sp"
        android:textStyle="bold"
        android:background="@drawable/pill_dark"
        android:paddingHorizontal="14dp"
        android:paddingVertical="7dp"
        android:elevation="3dp" />

    <!-- 반복 횟수 카운터: 다른 화면 카드처럼 accent 테두리 배지 스타일로, 화면 오른쪽 위에 배치 -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="32dp"
        android:layout_marginEnd="16dp"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/pill_dark_accent_border"
        android:paddingHorizontal="18dp"
        android:paddingVertical="8dp"
        android:elevation="3dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="완료 개수"
            android:textColor="@color/text_secondary"
            android:textSize="11sp" />

        <TextView
            android:id="@+id/repCountText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textColor="@color/accent"
            android:textSize="24sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- 피드백 텍스트 (화면 하단, 오버레이 위) -->
    <TextView
        android:id="@+id/feedbackText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="86dp"
        android:gravity="center"
        android:text="자세 피드백이 여기에 표시됩니다"
        android:textColor="@color/text_primary"
        android:textSize="16sp"
        android:textStyle="bold"
        android:background="@drawable/pill_dark"
        android:paddingHorizontal="18dp"
        android:paddingVertical="10dp"
        android:elevation="3dp" />

    <!-- 닫기 버튼 (화면 맨 아래) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnClosePose"
        style="@style/SmartBulk.Button.Outlined"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="닫기"
        android:layout_gravity="bottom"
        android:layout_margin="16dp"
        android:backgroundTint="#B3161C12"
        android:elevation="4dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
