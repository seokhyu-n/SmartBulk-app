<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartbulk">

    <!-- ✅ Firebase용 인터넷 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ✅ 카메라 권한 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartBulk"> <!-- ✅ 전체 앱 테마 -->

        <!-- ✅ 로그인 액티비티 (앱 시작 화면) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartBulk.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ 메인 액티비티 -->
        <activity android:name=".MainActivity" />

        <!-- ✅ 회원가입 액티비티 -->
        <activity android:name=".RegisterActivity" />

        <!-- ✅ 마이페이지 액티비티 -->
        <activity android:name=".MyPageActivity" />

        <!-- ✅ 운동 상세 액티비티 -->
        <activity android:name=".WorkoutDetailActivity" />

        <!-- ✅ 운동 종목 상세 (운동 설명 + 피드백 버튼) -->
        <activity android:name=".ExerciseDetailActivity" />

        <!-- ✅ 통계 화면 액티비티 -->
        <activity android:name=".StatisticsActivity" />

        <!-- ✅ 설정 화면 액티비티 -->
        <activity android:name=".SettingsActivity" />

        <!-- ✅ 새로 추가: AI 자세 피드백 전용 액티비티 -->
        <activity android:name=".PoseFeedbackActivity" />

    </application>
</manifest>
